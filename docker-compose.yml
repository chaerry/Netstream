version: '3.8'

services:
  inventory-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: netstream-inventory-backend
    ports:
      - "8071:8071"
    environment:
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://145.79.8.141:5433/netstream
      QUARKUS_DATASOURCE_USERNAME: n8n
      QUARKUS_DATASOURCE_PASSWORD: rahasia
      QUARKUS_OIDC_AUTH_SERVER_URL: https://keycloak.aitiserve.co.id:8095/realms/aitiserve
      QUARKUS_OIDC_CLIENT_ID: itsm_gaharu
      QUARKUS_OIDC_CREDENTIALS_SECRET: itsm123
      QUARKUS_HTTP_PORT: 8071
    restart: unless-stopped

  inventory-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: netstream-inventory-frontend
    ports:
      - "5173:80"
    depends_on:
      - inventory-backend
    restart: unless-stopped
